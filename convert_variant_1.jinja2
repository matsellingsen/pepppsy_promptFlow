{# Prompt is a jinja2 template that generates prompt for LLM #}

system:
Your task is convert a given text into a more readable and empathic version. Do not remove or add information during the convertion.

user:
Here are a few examples:
{% for ex in examples %}
Text content: {{ex.text}}
OUTPUT: {"{{ex.text_converted}}"}
{% endfor %}

For a given text, convert the text into a more readable and empathic version. :
Text content: {{input.text}}
OUTPUT:

